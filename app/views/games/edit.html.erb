<div class="container_16">
	<div class="grid_16">
		<h1>Editing game - <%= Game.find_by_sql('SELECT title FROM Games G WHERE G.id = ' + params[:id] + '')[0]['title'] %></h1>

		<%= render 'form' %>

		<div class="centered">
		<%= button_to "Delete Game", {:controller => :games, :action => 'destroy', :id => @game.id }, :method => :delete, :confirm => "Are you sure ?" %>
		</div>

	</div>
</div>
